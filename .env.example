# Dataverse API Configuration Template
# Copy this file to .env and update with your values
# DO NOT commit .env to git (it's in .gitignore)

# Dataverse API Base URL (must include /api/data/v9.2)
# Replace with your actual Dataverse environment URL
VITE_DATAVERSE_URL=https://yourorg.crm.dynamics.com/api/data/v9.2

# Optional: Use mock data instead of real Dataverse (for development)
# Set to 'true' to use mock data, 'false' or omit to use real Dataverse
# VITE_USE_MOCK_DATA=false

# Optional: Logging level
# Options: DEBUG, INFO, WARN, ERROR
# VITE_LOG_LEVEL=DEBUG
